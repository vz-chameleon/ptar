#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <unistd.h>


#include "ptar.h"


int main(int argc, char** argv){
  
  char oct[]="coucou";
  strcpy(oct,argv[1]);

 int octalNumber= octal_to_decimal(oct);
 
 printf("%d",octalNumber);

   return 0;
} 
